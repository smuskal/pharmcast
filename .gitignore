__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.pytest_cache/
.venv/
venv/
.DS_Store

# Model weights are large and are attached to releases, not committed.
*.pt

# Corpus data is never committed — only the trained parameters are released.
*.pfp
*.pfp.gz
# ...but keep the small decoder example, which is useless without its input
!tools/pfp-decode/examples/*.pfp
*.sdf
*.smi
# ...except the small example inputs, which the worked scripts need
!examples/data/*.smi
!examples/*.smi
